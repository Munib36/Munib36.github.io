import "../css/nav.css"

export default function Nav(){
    return(
        <nav>
            <a href="#">Home</a>
            <a href="#">Contact</a>
            <a href="#">About us</a>
            <a href="#">Why</a>
        </nav>
    )
}